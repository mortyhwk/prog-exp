export const enum PageIds {
    SignIn,
    SignUp,
    SignUpContinue,
    PasswordRecovery,
    Rules
}

export const enum ButtonType {
    Default,
    Transparent,
    TransparentCheckbox,
    Arrow
}

export const enum Rules {
    NoRead,
    Read
}
